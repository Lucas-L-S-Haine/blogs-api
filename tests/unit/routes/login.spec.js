const { fail } = require('assert/strict');
const router = require('../../../src/routes/loginRouter');

console.log(router);

// loginRouter.post('/', login);
describe('Test login router', () => {
  it('should use "/login" route', () => {
    fail('test has yet to be implemented');
  });
});
